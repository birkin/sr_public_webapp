Django==4.2.14
git+https://github.com/birkin/shellvars-py.git
pip-tools==6.13.0
requests==2.32.2  # can't go higher, because it requires urllib3 2.x, which requires a version of open-ssl newer than the one on our servers.
trio==0.22.0
